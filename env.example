# Database
MONGODB_URI=mongodb://localhost:27017/teddy-shop
# Or MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/teddy-shop

# Authentication
# ⚠️ CRITICAL: Generate a unique secret for YOUR project!
# DO NOT use this example value - it's publicly visible!
AUTH_SECRET=EXAMPLE_ONLY_abc123XYZ789_GENERATE_YOUR_OWN_WITH_OPENSSL
NEXTAUTH_URL=http://localhost:3000
# Generate YOUR secret with: openssl rand -base64 32

# Admin Credentials
# ⚠️ WARNING: These are PLACEHOLDER values! MUST be changed before first run!
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=your-strong-password-here
# Password will be hashed with bcrypt, not stored in plain text

# Site URL (for metadata, social sharing)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Production: https://yourdomain.com

# Vercel Blob Storage (for file uploads)
# ⚠️ REQUIRED: Create Blob store first, then get token
# Steps:
# 1. Go to: https://vercel.com/dashboard
# 2. Select your project → Storage → Blob
# 3. Click "Create Store" (if not exists)
# 4. Copy the BLOB_READ_WRITE_TOKEN
# 5. Paste it below
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# This token is required for uploading/deleting files (images, media, etc.)
# Error "This store does not exist" means you need to create the Blob store first!

# Cloudflare Turnstile CAPTCHA (for comment system)
# Development: Use test key (always passes)
NEXT_PUBLIC_TURNSTILE_SITE_KEY=1x00000000000000000000AA
# Server-side verification secret (test value for dev)
TURNSTILE_SECRET_KEY=1x0000000000000000000000000000000AA
# Production: Get real key from https://dash.cloudflare.com/
# Test Keys (for development):
# - Site Key: 1x00000000000000000000AA (always passes)
# - Secret Key: 1x0000000000000000000000000000000AA (for server-side verification)